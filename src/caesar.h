#include <string>

int getdif(std::string letters);

char move(char letter, int distance);

std::string converse(std::string cipher, int shift);

std::string caesar(std::string mode, std::string text, std::string shift);